/**
 * Re-export the endpoint builders from `constants/endpoints.js` so services
 * can import everything from one place if they prefer.
 */

export { ENDPOINTS, buildQuery } from '../../constants/endpoints';
